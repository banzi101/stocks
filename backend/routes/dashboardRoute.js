const express = require('express');
const router = express.Router();
const dashboardController = require('../controllers/dashboardController');

router.get("/________", dashboardController._________);

module.exports = router;
