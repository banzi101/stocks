{
  "projects": {
    "backend": {
      "root": "api",
      "buildCommand": "npm install && npm run build",
      "outputDirectory": "build",
      "devCommand": "npm run dev"
    },
    "frontend": {
      "root": "client",
      "buildCommand": "npm install && npm run build",
      "outputDirectory": "build",
      "devCommand": "npm run dev"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
